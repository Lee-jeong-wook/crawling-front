import styled from "styled-components"

export const List = () => {
    return (
        <StyledListContainer>
        
        </StyledListContainer>
    )
}

const StyledListContainer = styled.div`
    
`